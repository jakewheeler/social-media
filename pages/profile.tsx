import { Box } from '@chakra-ui/core';
import { FeedItemProps } from '../components/Feed';

type ProfileProps = {
  userSeed: string;
};

export default function Profile({ userSeed }: ProfileProps) {
  //   return <Box>{appUser.name}</Box>;
}
